<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blackjack Game</title>
    <link rel="stylesheet" href="./public/blackJack.css"> <!-- External CSS for styling -->
</head>
<body>
    <div class="game-container">
        <h1>Blackjack Game</h1>
        
        <div class="game-status">
            <p><strong>Wins:</strong> <%= wins %></p>
            <p><strong>Losses:</strong> <%= losses %></p>
        </div>

        <div class="cards-section">
            <h2>Your Hand</h2>
            <div class="player-cards">
                <% playerHand.forEach(card => { %>
                    <div class="card">
                        <p><%= card.value %> of <%= card.suit %></p>
                    </div>
                <% }); %>
            </div>
            <p><strong>Your Score:</strong> <%= playerScore %></p>
        </div>

        <div class="cards-section">
            <h2>Dealer's Hand</h2>
            <div class="dealer-cards">
                <% dealerHand.forEach((card, index) => { %>
                    <div class="card">
                        <p><%= index === 0 ? `${card.value} of ${card.suit}` : 'Hidden' %></p>
                    </div>
                <% }); %>
            </div>
            <p><strong>Dealer shows:</strong> <%= dealerHand[0].value %> of <%= dealerHand[0].suit %></p>
        </div>

        <div class="controls">
            <form action="/play" method="post">
                <button type="submit" name="action" value="hit">Hit</button>
                <button type="submit" name="action" value="stand">Stand</button>
            </form>
        </div>
    </div>
</body>
</html>
